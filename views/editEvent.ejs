<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Event</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style1.css">
</head>
<body class="bg-light d-flex justify-content-center align-items-center" style="min-height: 100vh;">

  <div class="card shadow p-4" style="width: 100%; max-width: 500px;">
    <h2 class="text-center mb-4">Edit Event</h2>

    <form method="POST" action="/organiser/edit/<%= event.id %>">
      <div class="mb-3">
        <label class="form-label">Title</label>
        <input type="text" name="title" class="form-control" value="<%= event.title %>" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Description</label>
        <textarea name="description" class="form-control" rows="4" required><%= event.description %></textarea>
      </div>
      <div class="mb-3">
        <label class="form-label">Date</label>
        <input type="date" name="date" class="form-control" value="<%= event.date %>" required>
      </div>

      <!-- Normal Tickets -->
      <div class="mb-3">
        <label>Normal Tickets</label>
        <input type="number" name="normalQty" class="form-control" value="<%= event.normal_quantity %>" min="0" required>
        <label>Price ($)</label>
        <input type="number" name="normalPrice" class="form-control" value="<%= event.normal_price %>" step="0.01" min="0" required>
      </div>

      <!-- Concession Tickets-->
      <div class="mb-3">
        <label>Concession Tickets</label>
        <input type="number" name="concessionQty" class="form-control" value="<%= event.concession_quantity %>" min="0" required>
        <label>Price ($)</label>
        <input type="number" name="concessionPrice" class="form-control" value="<%= event.concession_price %>" step="0.01" min="0" required>
      </div>

      <button type="submit" class="btn btn-primary w-100">Save Changes</button>
    </form>

    

    <div class="text-center mt-3">
      <a href="/organiser/home" class="text-decoration-none">← Back to Dashboard</a>
    </div>
  </div>

</body>
</html>

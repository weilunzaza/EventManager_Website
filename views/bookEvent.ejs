<!DOCTYPE html>
<html>
<head>
  <title>Book Event</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h2>Book Tickets for <%= event.title %></h2>
    <p><%= event.description %></p>
    <p><strong>Date:</strong> <%= event.date %></p>
    <p><strong>Organiser:</strong> <%= event.organiser_name || "N/A" %> (<%= event.organiser_company || "N/A" %>)</p>

    <form method="POST" action="/attendee/book/<%= event.id %>">
      <div class="mb-3">
        <label>Full Name</label>
        <input type="text" name="fullName" class="form-control" required autofocus>
      </div>
      <div class="mb-3">
        <label>Email</label>
        <input type="email" name="email" class="form-control" required>
      </div>

      <div class="mb-3">
        <label>Normal Tickets ($<%= event.normalPrice %> each) - Available: <%= event.normalQty %></label>
        <input type="number" name="normalQty" class="form-control" min="0" max="<%= event.normalQty %>" value="0">
      </div>
      <div class="mb-3">
        <label>Concession Tickets ($<%= event.concessionPrice %> each) - Available: <%= event.concessionQty %></label>
        <input type="number" name="concessionQty" class="form-control" min="0" max="<%= event.concessionQty %>" value="0">
      </div>

      <button type="submit" class="btn btn-success">Confirm Booking</button>
    </form>

    <div class="mt-3">
      <a href="/attendee/home" class="btn btn-primary mt-2">← Back to Events</a>
    </div>
  </div>
</body>
</html>

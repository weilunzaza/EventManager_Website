<!DOCTYPE html>
<html>
<head>
  <title>Book Event</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .form-section {
      background-color: #ffffff;
      border-radius: 10px;
      padding: 30px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.05);
    }
    .ticket-label {
      font-weight: 500;
    }
  </style>
</head>
<body class="bg-light">

  <div class="container mt-5 mb-5">
    <div class="row justify-content-center">
      <div class="col-lg-8">

        <div class="form-section">
          <h2 class="mb-3 text-center text-primary">🎟️ Book Tickets for "<%= event.title %>"</h2>

          <p class="text-muted mb-1"><%= event.description %></p>
          <p><strong>Date:</strong> <%= event.date %></p>
          <p><strong>Organiser:</strong> <%= event.organiser_name || "N/A" %> (<%= event.organiser_company || "N/A" %>)</p>

          <hr>

          <form method="POST" action="/attendee/checkout/<%= event.id %>">
            <h5 class="mb-3">Your Information</h5>
            <div class="mb-3">
              <label class="form-label">Full Name</label>
              <input type="text" name="fullName" class="form-control" required autofocus>
            </div>
            <div class="mb-4">
              <label class="form-label">Email</label>
              <input type="email" name="email" class="form-control" required>
            </div>

            <h5 class="mb-3">Ticket Selection</h5>
            <div class="mb-3">
              <label class="form-label ticket-label">
                Normal Tickets ($<%= event.normalPrice %> each)
                <small class="text-muted">– Available: <%= event.normalQty %></small>
              </label>
              <input type="number" name="normalQty" class="form-control" min="0" max="<%= event.normalQty %>" value="0">
            </div>

            <div class="mb-4">
              <label class="form-label ticket-label">
                Concession Tickets ($<%= event.concessionPrice %> each)
                <small class="text-muted">– Available: <%= event.concessionQty %></small>
              </label>
              <input type="number" name="concessionQty" class="form-control" min="0" max="<%= event.concessionQty %>" value="0">
            </div>

            <div class="d-grid">
              <button type="submit" class="btn btn-success btn-lg">Proceed to Checkout</button>
            </div>
          </form>

          <div class="text-center mt-4">
            <a href="/attendee/home" class="btn btn-outline-secondary">← Back to Events</a>
          </div>
        </div>

      </div>
    </div>
  </div>

</body>
</html>

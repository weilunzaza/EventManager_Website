<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Organiser Settings</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light d-flex justify-content-center align-items-center" style="min-height: 100vh;">

  <div class="card shadow p-4" style="width: 100%; max-width: 600px;">
    <h2 class="text-center mb-4">Organiser Settings Page</h2>

    <form method="POST" action="/organiser/settings">
      <div class="mb-3">
        <label class="form-label fw-bold">Organiser Title</label>
        <input type="text" name="site_title" class="form-control" value="<%= settings.site_title || '' %>" placeholder="e.g. EventManager 3000" required autofocus>
      </div>

      <div class="mb-3">
        <label class="form-label fw-bold">Organiser Name</label>
        <input type="text" name="organiser_name" class="form-control" value="<%= settings.organiser_name || '' %>" placeholder="e.g. Alice Tan" required>
      </div>

      <div class="mb-3">
        <label class="form-label fw-bold">Organiser Company</label>
        <input type="text" name="organiser_company" class="form-control" value="<%= settings.organiser_company || '' %>" placeholder="e.g. Alpha Events Pte Ltd" required>
      </div>

      <button type="submit" class="btn btn-primary w-100">Save Settings</button>
    </form>

    <div class="text-center mt-3">
      <a href="/organiser/home" class="text-decoration-none">← Back to Dashboard</a>
    </div>
  </div>

</body>
</html>

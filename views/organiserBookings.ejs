<!DOCTYPE html>
<html>
<head>
  <title>Organiser Bookings</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container my-5">
    <div class="card shadow p-4">
      <h2 class="mb-4">📋 Bookings Summary</h2>

      <% if (bookings.length === 0) { %>
        <p class="text-muted">No bookings yet.</p>
      <% } else { %>
        <table class="table table-bordered table-striped">
          <thead class="table-light">
            <tr>
              <th>Event</th>
              <th>Attendee</th>
              <th>Email</th>
              <th>Normal</th>
              <th>Concession</th>
              <th>Total</th>
              <th>Time</th>
            </tr>
          </thead>
          <tbody>
            <% bookings.forEach(booking => { %>
              <tr>
                <td><%= booking.event_title %></td>
                <td><%= booking.full_name %></td>
                <td><%= booking.email %></td>
                <td><%= booking.normal_qty %></td>
                <td><%= booking.concession_qty %></td>
                <td>$<%= (booking.normal_qty * 10 + booking.concession_qty * 5).toFixed(2) %></td>
                <td><%= booking.created_at %></td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      <% } %>

      <a href="/organiser/home" class="btn btn-secondary mt-3">← Back to Dashboard</a>
    </div>
  </div>
</body>
</html>
